import { colors, gradients } from './theme';

export const theme = {
  colors,
  gradients,
  spacing: {
    xs: 8,
    sm: 12,
    md: 16,
    lg: 24,
    xl: 32,
  },
  radius: {
    sm: 12,
    md: 20,
    lg: 28,
  },
};
